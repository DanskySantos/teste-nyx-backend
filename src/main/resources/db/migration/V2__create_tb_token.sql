create table token
(
    id                      bigint generated by default as identity,
    creation_date           timestamp,
    update_date             timestamp,
    uuid                    varchar(510),
    token                   varchar(510),
    token_type              varchar(510),
    revoked                 boolean,
    expired                 boolean,
    user_id                 bigint,
    primary key (id)
);

alter table token
    add constraint FK349pmtlc4ukgn1so04k2lstyu foreign key (user_id) references tb_user;
